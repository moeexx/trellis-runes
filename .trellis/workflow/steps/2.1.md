# 2.1 实现 `[required · repeatable]`

## 目标

将已评审的规划产物实现为代码，并遵循注入的 spec 与研究结论。

## 执行动作

具备 sub-agent 支持的平台派发 `trellis-implement`：

- prompt 必须以 `Active task: <task path>` 开头。
- 明确 agent 已是 `trellis-implement`，不得再次派发 implement/check agent。
- 读取 `implement.jsonl`、`prd.md`、`design.md`、`implement.md` 和相关研究。
- 保持改动聚焦，不修改无关文件。
- 完成后运行项目 lint、type-check 或等价验证。

不具备 sub-agent 支持的平台，先加载 `trellis-before-dev`，再由主会话直接实现。

## 产物与状态

- 代码实现完成。
- 相关测试和静态检查已执行，结果可复现。

## 验证标准

- 实现符合 PRD、设计和相关 spec。
- 没有通过跳过检查、吞错误或削弱测试制造假通过。

## 下一步

实现完成后进入 [2.2](./2.2.md)。
